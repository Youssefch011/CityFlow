# CityFlow Futuristic UI — Image Cards Version

This version shows destination images directly inside cards.

Put your images here:

assets/images/

The filenames must match the `image` column in:

data/locations.csv

Run:

streamlit run app.py
